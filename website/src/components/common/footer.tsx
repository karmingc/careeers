/** @jsx jsx */
import { css, jsx } from '@emotion/core';

const Footer: React.FC = () => {
  return (
    <footer
      css={css`
        margin: 50px;
      `}
    ></footer>
  );
};

export default Footer;
